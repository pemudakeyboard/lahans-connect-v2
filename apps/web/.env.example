# LAHANS CONNECT WEB — environment template
# Public-facing values only (bundle-safe). Nothing secret here.

# API base used by the dev proxy (next.config rewrites) and by client fetches.
# In dev, leave empty to default to http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001